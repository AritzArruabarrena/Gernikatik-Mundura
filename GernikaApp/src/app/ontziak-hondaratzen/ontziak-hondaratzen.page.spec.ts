import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OntziakHondaratzenPage } from './ontziak-hondaratzen.page';

describe('OntziakHondaratzenPage', () => {
  let component: OntziakHondaratzenPage;
  let fixture: ComponentFixture<OntziakHondaratzenPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OntziakHondaratzenPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
