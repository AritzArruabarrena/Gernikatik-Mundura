import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MarihitzegitenPage } from './marihitzegiten.page';

describe('MarihitzegitenPage', () => {
  let component: MarihitzegitenPage;
  let fixture: ComponentFixture<MarihitzegitenPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MarihitzegitenPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
