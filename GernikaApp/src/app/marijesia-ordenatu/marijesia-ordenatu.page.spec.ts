import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MarijesiaOrdenatuPage } from './marijesia-ordenatu.page';

describe('MarijesiaOrdenatuPage', () => {
  let component: MarijesiaOrdenatuPage;
  let fixture: ComponentFixture<MarijesiaOrdenatuPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MarijesiaOrdenatuPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
