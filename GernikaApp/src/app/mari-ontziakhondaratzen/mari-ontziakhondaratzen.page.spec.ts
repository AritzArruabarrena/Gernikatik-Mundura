import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MariOntziakhondaratzenPage } from './mari-ontziakhondaratzen.page';

describe('MariOntziakhondaratzenPage', () => {
  let component: MariOntziakhondaratzenPage;
  let fixture: ComponentFixture<MariOntziakhondaratzenPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MariOntziakhondaratzenPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
