import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MarijesiakHutsuneakBete1Page } from './marijesiak-hutsuneak-bete-1.page';

describe('MarijesiakHutsuneakBete1Page', () => {
  let component: MarijesiakHutsuneakBete1Page;
  let fixture: ComponentFixture<MarijesiakHutsuneakBete1Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MarijesiakHutsuneakBete1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
