import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MarijesiakHutsuneakBetePage } from './marijesiak-hutsuneak-bete.page';

describe('MarijesiakHutsuneakBetePage', () => {
  let component: MarijesiakHutsuneakBetePage;
  let fixture: ComponentFixture<MarijesiakHutsuneakBetePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MarijesiakHutsuneakBetePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
