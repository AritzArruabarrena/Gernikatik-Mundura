import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MariGalderakerantzutenPage } from './mari-galderakerantzuten.page';

describe('MariGalderakerantzutenPage', () => {
  let component: MariGalderakerantzutenPage;
  let fixture: ComponentFixture<MariGalderakerantzutenPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MariGalderakerantzutenPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
